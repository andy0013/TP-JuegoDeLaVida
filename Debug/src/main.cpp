/*
 * main.cpp
 *
 *  Created on: 31 oct. 2018
 *      Author: leandro
 */

#include <iostream>
#include "Lector.h"
#include "Contador.h"
#include "ControlJuego.h"
#include "Lista.h"

using namespace std;
typedef std::string palabra;

int main(){
   /* Contador contador;
    Lector lector;
    Lista<Tablero*>* listaTableros = lector.leerNotasTablero();

    listaTableros->iniciarCursor();
        while (listaTableros->avanzarCursor()){
            Tablero* tableroActual = listaTableros->obtenerCursor();
            int celulasVivas = contador.contarCelulasVivas(tableroActual);
            std::string nombreTablero = tableroActual->obtenerNombreTablero();
            cout << "celulas vivas en el tablero " << nombreTablero << " " << celulasVivas << endl;
            int c = contador.contarCelulasVecinas(tableroActual, 1, 2);
            cout << "celulas alrededor del (1,2) en el tablero " << c << endl;

        }
*/
}

